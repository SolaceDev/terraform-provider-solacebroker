version: 3

project:
  locator: SolaceDev_terraform-provider-solacebroker
  id: SolaceDev_terraform-provider-solacebroker
  name: terraform-provider-solacebroker
  teams: []
  labels:
    - go-dependencies
    - terraform-provider

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    only:
      - "**/LICENSE"
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
